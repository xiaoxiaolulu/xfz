#!/usr/bin/env node
console.log(`
	----------------------
	-- SECURITY WARNING --
	----------------------

	If you see this message, re-evaluate your current security settings.
	You're seeing this because you likely meant to run:

	  $ npx gulp

	but instead ran:

	  $ npx gulo

	Hence, you're allowing to run arbitrary code on your machine.
	Sensitive information could have been compromised at this point.
`);